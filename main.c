#include "lib.h"

int main(){
  //use a windows manager to create a window, and interact with it
  /*
  #code here
  */
  //when the interaction that causes the call to start happen, call Setup_Call to make it happen
  Setup_Call("::1", 8888, "::1", 8889);
}